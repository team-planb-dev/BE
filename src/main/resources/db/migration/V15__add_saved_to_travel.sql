-- ============================================================================
-- travel: add saved (저장 확정 여부)
-- ============================================================================
-- 이미 저장되어 있던 기존 여행은 저장 확정 상태로 간주한다.
ALTER TABLE travel
    ADD COLUMN saved VARCHAR(1) NOT NULL DEFAULT 'Y';
